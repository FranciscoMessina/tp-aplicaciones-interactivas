info:
  name: Obtener producto
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/products/:id"
  params:
    - name: id
      value: ""
      type: path
      description: Id del producto, copiarlo de "Listar productos"
  auth: inherit

settings:
  encodeUrl: true
