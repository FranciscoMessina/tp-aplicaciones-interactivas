info:
  name: Eliminar consulta
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/enquiries/{{enquiryId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("elimina la consulta", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
