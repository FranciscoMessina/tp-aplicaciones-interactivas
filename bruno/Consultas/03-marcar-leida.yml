info:
  name: Marcar consulta como leida
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/enquiries/{{enquiryId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "READ"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("avanza la consulta a READ", function() {
          expect(res.status).to.equal(200);
          expect(res.body.status).to.equal("READ");
        });

settings:
  encodeUrl: true
