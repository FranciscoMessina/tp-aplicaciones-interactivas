info:
  name: Obtener informacion del negocio
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/business-info"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("obtiene la informacion publica del negocio", function() {
          expect(res.status).to.equal(200);
          expect(res.body.name).to.equal("Comercio Bruno");
          expect(res.body.address).to.be.a("string");
        });

settings:
  encodeUrl: true
