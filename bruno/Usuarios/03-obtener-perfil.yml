info:
  name: Obtener perfil
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/users/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("obtiene el perfil autenticado", function() {
          expect(res.status).to.equal(200);
          expect(res.body.user.email).to.equal(bru.getVar("userEmail"));
        });

settings:
  encodeUrl: true
