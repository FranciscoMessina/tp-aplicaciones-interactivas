info:
  name: Eliminar categoria
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/categories/:id"
  params:
    - name: id
      value: ""
      type: path
      description: Id de la categoria, copiarlo de "Listar categorias"
  auth: inherit

docs: |-
  Una categoria con productos no se puede eliminar.

settings:
  encodeUrl: true
