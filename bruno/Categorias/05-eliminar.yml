info:
  name: Eliminar categoria
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/categories/{{disposableCategoryId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("elimina una categoria sin productos asociados", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
