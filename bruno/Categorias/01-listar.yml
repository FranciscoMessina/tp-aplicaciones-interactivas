info:
  name: Listar categorias
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/categories"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("lista las categorias", function() {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("array");
        });

settings:
  encodeUrl: true
